vlib work
vmap work work

# ?????????? RTL (?? rtl)
vlog -sv rtl/*.sv

# ?????????? testbench (?? sim/src)
vlog -sv sim/src/*.sv

# ?????? ?????????
vsim -gui -voptargs=+acc work.tb
log sim/src/tb*
add wave sim/src/tb*

add wave -r sim:/tb/*
run -all
